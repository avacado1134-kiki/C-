#include <stdio.h>

int main(){
    int condition=0;
    do
    {
        /* code */
        printf("the execution at most once\n");
    } while (condition);
    

    return 0;
}

//the do while loop exexuted for at most once