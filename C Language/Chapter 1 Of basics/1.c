#include <stdio.h>
int main(){
    int a;
    a=6 ;
    printf(" The output of the program is %d",a);
    // %d ka mtlb as an integer mujhe (a) dedo
    int b;
    scanf("%d", &b);
    // ek user se input lene ke liye kuch aisi line likhni hoti hai
    // address of b mein yeh jo user ke dwara enter ki gyi value hai usko le lerha hai
    
    printf(" the value of b is %d", b);
    return 0;
}
/* Keywords
auto,break,case,char,const,continue,default,do
double,long,return,register,short,signed,sizeof,static,int,else,enum,extern
float,for,goto,if,struct,switch,tyoeof,union,unsigned,void,volatile,while*/



