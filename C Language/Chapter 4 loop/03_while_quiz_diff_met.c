#include <stdio.h>

int main()
{
    int i = 0; // if given ki initialise i=0
    while (i <= 20)
    {
        if (i >= 10)
        {
            printf("the natural no. is %d \n", i);
        }
       
        
        i++;
        
    }
    return 0;
}